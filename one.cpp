#include <iostream>  // Preprocessor directive for input-output stream

using namespace std;  // Allows usage of names for objects and variables from the standard library

int main() {
    cout << "Hello, World!" << endl;  // Outputs "Hello, World!" followed by a newline
    return 0;  // Returns 0 to indicate that the program ended successfully
}
